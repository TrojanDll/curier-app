package com.example.curier_mobile.data.remote.api

/**
 * Retrofit API сервис для взаимодействия с backend
 *
 * Endpoints будут добавлены в следующих итерациях:
 * - Authentication (login, logout, refresh)
 * - Profile (get, update)
 * - Orders (get active, get history, update status, upload photo)
 * - Statistics (get courier statistics)
 */
interface ApiService {
    // TODO: Добавить endpoints в Iteration 3: Network Layer
}
