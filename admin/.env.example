# Copy this file to .env.local and adjust values for your environment.

# Origin of the NestJS backend. Server-only — used by Next BFF route
# handlers in admin/src/app/api/ to forward browser requests. The browser
# itself never sees this URL (no `NEXT_PUBLIC_` prefix).
BACKEND_API_URL=http://localhost:8081
