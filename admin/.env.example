# Copy this file to .env.local and adjust values for your environment.

# Backend HTTP origin. NEXT_PUBLIC_* инлайнится в клиентский bundle.
# В dev backend крутится на 8081 (см. backend/.env.example).
NEXT_PUBLIC_API_URL=http://localhost:8081
